#include "push_swap.h"

int	main(int argv, char **argc)
{
	//t_list	*a;
	//t_list	*b;
	int		len;


	len = check_err(argv, argc);
	if (!len)
		return (0);
	//a = (arg_list *)malloc(sizeof(arg_list) * len);
	//b = (arg_list *)malloc(sizeof(arg_list) * len);
	//a = fill_a(argc, len);
}
